# RBAC for the Spark-on-Kubernetes baseline run.
#
# In cluster mode the driver runs as a pod and creates/deletes its own executor
# pods through the API server, so it needs a ServiceAccount with rights over
# pods (and configmaps, which Spark uses to ship driver conf to executors).
# Scoped to a Role in one namespace, not a ClusterRole — the driver has no
# business seeing anything outside its own run.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: spark-bench
  namespace: krishiv-apitest
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: spark-bench
  namespace: krishiv-apitest
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete", "deletecollection"]
  - apiGroups: [""]
    resources: ["pods/log", "pods/status"]
    verbs: ["get", "list", "watch"]
  - apiGroups: [""]
    resources: ["services"]
    # `patch` is required: Spark 3.5 creates the driver service with server-side
    # apply (PATCH ?fieldManager=fabric8&force=true), not POST. Without it the
    # submission authenticates fine and then dies 403 on the driver service —
    # after the driver pod has already been created.
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete", "deletecollection"]
  - apiGroups: [""]
    resources: ["configmaps"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete", "deletecollection"]
  - apiGroups: [""]
    resources: ["persistentvolumeclaims"]
    verbs: ["get", "list", "watch", "create", "update", "patch", "delete", "deletecollection"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: spark-bench
  namespace: krishiv-apitest
subjects:
  - kind: ServiceAccount
    name: spark-bench
    namespace: krishiv-apitest
roleRef:
  kind: Role
  name: spark-bench
  apiGroup: rbac.authorization.k8s.io
