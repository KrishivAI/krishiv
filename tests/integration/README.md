# Integration Tests

Workspace-level integration scenarios will live here once the root workspace has runnable cross-crate flows.

Current R1 bootstrap tests live inside individual crates so `cargo test --workspace` can run them without a root package.
