apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Root kustomization — delegates to the operator overlay.
# For direct (no-operator) deployment use k8s/direct/ instead.
resources:
  - operator
