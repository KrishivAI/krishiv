target/
.git/
.github/
.claude/
docs/
scripts/
deploy/
examples/
dist/
# ...except the staged binaries Dockerfile.fast COPYs. `just docker-fast` and
# `just stage` both write here; without this negation both COPY lines miss the
# build context and the image fails on "dist/docker/krishiv: not found".
!dist/docker/
.bench-baselines/
venv/
tpch*
*.md
*.sh
*.toml.bak
justfile
.cargo/
rust-toolchain.toml
flamegraph/
perf.data
perf.data.old
