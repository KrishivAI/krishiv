[target.x86_64-unknown-linux-gnu]
rustflags = [
    "-C", "link-arg=-fuse-ld=lld",
    "-C", "target-cpu=native",
]

# Avoid /tmp quota/race failures during native dependency builds (ring, aws-lc-sys, protoc).
[env]
TMPDIR = { value = ".tmp", relative = true }
