---
title: CLI
description: Krishiv CLI commands for SQL, plans, jobs, and local clusters.
---

# CLI

```bash
cargo run -p krishiv -- sql --query "SELECT 1 AS value"
cargo run -p krishiv -- explain --query "SELECT 1 AS value"
cargo run -p krishiv -- jobs
```
