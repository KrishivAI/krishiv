---
title: CLI reference
description: Run SQL, inspect plans, and manage local Engine processes from a source checkout.
status: available
---

The `krishiv` binary is the command-line front door for queries, local
development, and daemon operations. Krishiv is pre-release, so the commands
below run the binary from a source checkout rather than assuming a published
package is available.

## Quick start

<Steps>
  <Step>

### Build and inspect the command surface

```bash
cargo build -p krishiv
cargo run -p krishiv -- --help
```

  </Step>
  <Step>

### Run an embedded query

```bash
cargo run -p krishiv -- sql --query "SELECT 42 AS answer"
```

  </Step>
</Steps>

## Quick index

<Cards>
  <Card
    title="Command map"
    description="Find the command family that owns a task."
    href="#command-map"
  />
  <Card
    title="Query and plan"
    description="Review SQL flags, placement, and common query examples."
    href="#query-and-plan"
  />
  <Card
    title="Local development cluster"
    description="Start, inspect, and stop a single-host development topology."
    href="#local-development-cluster"
  />
  <Card
    title="Configuration reference"
    description="Resolve placement, durability, authentication, and tuning settings."
    href="/docs/engine/reference/configuration"
  />
</Cards>

## Command map

| Command | Purpose |
| --- | --- |
| `sql` | Plan and run SQL. Embedded mode is the default. |
| `explain` | Show logical and physical plan information. |
| `table` | Work with Parquet and optional lakehouse table paths. |
| `stream` | Register, push to, and poll continuous stream jobs. |
| `ivm` | Operate experimental incremental-view jobs. |
| `pipeline` | Work with declarative source-to-sink pipelines. |
| `doctor` | Print the effective deployment configuration. |
| `local` | Start, inspect, or stop a single-host development cluster. |
| `cluster` | Operate a process-managed distributed topology. |
| `state`, `checkpoints`, `savepoint`, `restore` | Inspect and control stateful jobs. |

Use `cargo run -p krishiv -- <command> --help` for the options compiled into
your checkout.

## Query and plan

### SQL options

```text
krishiv sql --query <SQL> [OPTIONS]

-q, --query <SQL>       SQL statement or semicolon-separated statements
--mode <MODE>           embedded, single-node, or distributed
--parquet <NAME=PATH>   register a local Parquet file; repeatable
--local                 force the local execution entry point
--remote                use the configured remote coordinator
--timeout <SECONDS>     accepted for forward compatibility; not yet enforced
--api-key <KEY>         use the policy-enforced SQL entry point
```

For `sql`, the top-level `-c, --coordinator <URL>` option overrides the
canonical `KRISHIV_COORDINATOR_URL` setting. The `explain` dispatcher does not
currently receive that top-level override; set `KRISHIV_COORDINATOR_URL` if you
deliberately inspect a remote plan. A parsed `--timeout` does not currently
cancel work, so do not use it as an operational safeguard.

### Examples

<Tabs items={["Parquet", "Multi-statement", "Remote"]}>
  <Tab value="Parquet">

```bash
cargo run -p krishiv -- sql \
  --parquet events=./events.parquet \
  --query "SELECT kind, COUNT(*) AS n FROM events GROUP BY kind"
```

The registration exists only for this process.

  </Tab>
  <Tab value="Multi-statement">

```bash
cargo run -p krishiv -- sql --query \
  "CREATE TABLE t AS SELECT 1 AS id; SELECT * FROM t"
```

Earlier statements run for side effects. Only the last result is printed.

  </Tab>
  <Tab value="Remote">

```bash
cargo run -p krishiv -- \
  --coordinator http://127.0.0.1:2003 \
  sql --remote --query "SELECT 42 AS answer"
```

The endpoint must already expose a compatible Engine service.

  </Tab>
</Tabs>

## Local development cluster

<Steps>
  <Step>

### Start the cluster

```bash
cargo run -p krishiv -- local start --data-dir /tmp/krishiv-local
```

  </Step>
  <Step>

### Inspect its status

```bash
cargo run -p krishiv -- local status --data-dir /tmp/krishiv-local
```

  </Step>
  <Step>

### Stop the cluster

```bash
cargo run -p krishiv -- local stop --data-dir /tmp/krishiv-local
```

  </Step>
</Steps>

<Callout type="warn" title="CLI process lifetime">
  In-process job and catalog state does not survive a CLI invocation. Use a
  long-lived session or coordinator-backed lifecycle for persistent jobs.
</Callout>

## Automation and compatibility

<Callout type="info" title="Pin automated commands">
  The CLI and its configuration are preview APIs. Pin an Engine commit when
  automating commands, and check command help again when upgrading.
</Callout>

The CLI reports the options compiled into the current binary. Treat `--help`
from the pinned build as the final command-line reference for that checkout.

## Next steps

<Cards>
  <Card
    title="Run batch SQL"
    description="Register finite data, inspect a plan, and execute a query."
    href="/docs/engine/guides/batch-sql"
  />
  <Card
    title="Run a single node"
    description="Use the local coordinator, executor, and Flight SQL endpoint."
    href="/docs/engine/guides/single-node"
  />
  <Card
    title="Configure the runtime"
    description="Set placement, durability, state, and authentication values."
    href="/docs/engine/reference/configuration"
  />
  <Card
    title="Operate checkpoints"
    description="Understand checkpoint, savepoint, and restore boundaries."
    href="/docs/engine/operations/checkpointing"
  />
</Cards>
